/**
 * Implementation workspace reset (from scratch).
 * Add `packages/integration-framework`, bounded contexts under `src/contexts/`,
 * tests under `test/unit/`, and scripts per `docs/backlog.md` / `docs/plan-scrum.md`.
 */
export const IMPLEMENTATION_PENDING = true;
